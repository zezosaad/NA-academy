import 'package:flutter/material.dart';

class AppColors {
  // ─── Brand Palette: "Studio Editorial" ────────────────────────────────────
  // Iris indigo (primary) · Apricot coral (accent) · Mint sage (secondary)
  // Bone canvas · Linen sunken · Ink text — quiet, modern, literary.

  // ─── Light Theme ──────────────────────────────────────────────────────────
  static const Color bgCanvas = Color(0xFFF8F5EE); // Bone — warm paper white
  static const Color bgSurface = Color(0xFFFFFFFF); // Pure surface
  static const Color bgSunken = Color(0xFFEBE6DA); // Linen — recessed
  static const Color bgElevated = Color(0xFFFFFFFF);

  static const Color borderSubtle = Color(0x141B1D29); // Ink @ 8%
  static const Color borderStrong = Color(0x331B1D29); // Ink @ 20%

  static const Color textPrimary = Color(0xFF1B1D29); // Ink
  static const Color textSecondary = Color(0xFF555866);
  static const Color textMuted = Color(0xFF8A8D9A);

  static const Color accent = Color(0xFF5B5FCF); // Iris indigo (primary)
  static const Color accentSoft = Color(0xFFE3E4F8);
  static const Color accentDeep = Color(0xFF3F44A8);
  static const Color accentGlow = Color(0x4D5B5FCF); // 30% iris

  static const Color secondary = Color(0xFFFF8E72); // Apricot coral
  static const Color secondarySoft = Color(0xFFFFE0D5);

  static const Color success = Color(0xFF6FC9A1); // Mint sage
  static const Color warning = Color(0xFFE0A458); // Honey amber
  static const Color danger = Color(0xFFE5484D); // Crimson
  static const Color dangerSoft = Color(0xFFFCDADC);

  // ─── Dark Theme ───────────────────────────────────────────────────────────
  static const Color darkBgCanvas = Color(0xFF0F1018); // Deep ink night
  static const Color darkBgSurface = Color(0xFF181A26);
  static const Color darkBgSunken = Color(0xFF080911);
  static const Color darkBgElevated = Color(0xFF222533);

  static const Color darkBorderSubtle = Color(0x1AF8F5EE); // Bone @ 10%
  static const Color darkBorderStrong = Color(0x33F8F5EE); // Bone @ 20%

  static const Color darkTextPrimary = Color(0xFFF8F5EE); // Bone
  static const Color darkTextSecondary = Color(0xFFB8BAC8);
  static const Color darkTextMuted = Color(0xFF7A7D8C);

  static const Color darkAccent = Color(0xFF8B8FE8); // Lifted iris
  static const Color darkAccentSoft = Color(0xFF2B2E5C);
  static const Color darkAccentDeep = Color(0xFFC3C5F2);
  static const Color darkAccentGlow = Color(0x668B8FE8);

  static const Color darkSecondary = Color(0xFFFFA88F); // Lifted apricot
  static const Color darkSecondarySoft = Color(0xFF5A2E22);

  static const Color darkSuccess = Color(0xFF8FDDB8);
  static const Color darkWarning = Color(0xFFEEC282);
  static const Color darkDanger = Color(0xFFFF6E72);
  static const Color darkDangerSoft = Color(0xFF5A1F22);
}
