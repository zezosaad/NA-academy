# Paste once inside `http { }` in nginx.conf (before `include sites-enabled`), or:
#   echo 'include /var/opt/NA-academy/deploy/nginx/http-snippet-map.conf;' | sudo tee /etc/nginx/conf.d/00-upgrade-map.conf

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
